# 爪爪 · 完全解放版 — 本仓库脊骨

这是**独立实验仓库**，不是 爪爪 · Paw Work 产品。

- **不要**套用 `SELECT + DESCRIBE OUTCOME → DELIVER`、8 工具宪法、CWS 约束、Selection-first 产品法，或 PawWork-vnext 的 `AGENTS.md`。
- **取向**（不是产品法）：把已登录 Chrome 当成可编程层——智能油猴（Tampermonkey）祖先。开发者模式 only，不出售，不上架。
- **继续前先读**：[HANDOFF.md](HANDOFF.md)
- **加载根就是本文件夹**（有 `manifest.json`），不是 `artifacts/unpacked`。
- **禁止**把本仓库的 `action` 或解放层回并到 `PawWork-vnext` / 公开 `PawWork_ZhuaZhua` `unpacked`，除非 Yiteng 本人另说。
