# 阶段行动报告

日期：2026-09-05

本阶段目标：在现有 MV3 扩展架构上，把“已登录浏览器作为可编程计算环境”推进到更可靠、可组合、可验证的状态。本阶段已收尾（当时写于工作区未推送）。后续提交已入库；Design/Slides（tldraw）已从产品面删除，当前画布为 sheet / doc / web。现状以 [AGENTS.md](AGENTS.md) 为准。重新加载扩展后生效。

## 1. 改了什么、做了什么

### 工作区数据与编辑器保存

- 重做持久化提交顺序：OPFS 文件写入新的不可变路径，再用一次 IndexedDB 事务发布元数据，成功后回收旧文件。失败时保留待保存状态，使后续保存可以恢复。
- 增加 OPFS 不可用时的 IDB 文件字节回退，以及恢复后的迁移路径。暂时无法访问 OPFS 不会直接清空已有文件引用。
- 修复文档、表格在慢保存期间产生的新编辑可能未继续保存的问题；画布和网站保存串行执行，成功后才更新已保存基线。保存失败不再被当作成功回执。
- 为交付物加入 revision / expectedRevision。四种编辑器和常用 office 修改路径携带读取版本；不同内容的过期写入返回 ARTIFACT_CONFLICT。画布、网站有本地未保存修改时，不直接用远端刷新覆盖。相同内容的重复请求可以确认已完成。
- guest 直接修改已有交付物主文件时推进版本，避免编辑器继续使用失效的基线。

涉及的主要模块：`durableStore.js`、`artifacts.js`、`fs.js`、`officeTools.js`、四种 preview 编辑器及服务 RPC。

### 浏览器执行与沙箱可靠性

- 删除提前发送 sandbox ready 的旧 handshake 脚本，改为运行时安装消息监听器后再报告就绪。真实浏览器验证中，原先首次调用超时的问题得到修复。
- 浏览器调用增加 callId、sessionId、executionId、截止时间和取消登记。扩展侧 fetch 的取消涵盖正文读取；超时或停止会返回明确错误。
- 对已经派发的页面代码和 CDP 操作，明确返回“结果未知”，不把停止等待误报为撤销副作用。
- 限制网络正文读取大小，超限时取消读取；补充晚到的 sandbox 回调与计时器清理，保留 guest 错误码。
- 同一会话拒绝重叠执行。RPC 连接建立后的响应丢失不再自动重放可能产生副作用的请求。
- sys 入口检查调用方必须是本扩展 offscreen runtime；并发 CDP 调用共享附着过程，其他 debugger 占用时返回错误。
- 截图前后检查活动标签，降低把其他标签截图误标为目标的风险。

涉及的主要模块：`browserSysHost.js`、新增 `sysClient.js`、`tools.js`、`sessionWorkspaceService.js`、sandbox/runtime、background/offscreen RPC。

### 可组合的计算能力

- 增加 `sys.capabilities()`，报告当前运行环境的能力和限制，供 agent 在执行前判断。
- fetch / screenshot 支持 `saveTo`，将结果写入 guest 文件系统并返回小型文件回执。写入 `/artifacts` 可走现有交付物登记链路。
- 这使“获取数据 → 代码处理 → 交付文件”能够在工作区内连接起来，减少模型上下文承担二进制内容搬运。
- 更新架构说明和使用文档，最低 Chrome 版本改为 135；保留原审查记录，并新增 `BROWSER_COMPUTER.md` 描述实现与边界。

### 验证

本阶段最后一次运行结果：

- `node --test tests/runtime-regression.test.mjs`：13 项通过，0 项失败。覆盖提交失败恢复、OPFS 替换、慢保存、冲突与重试、取消、读取限额、能力探测、CDP 附着和文件回执。
- `tests/browser-smoke.cjs`：独立 Chromium 配置加载真实扩展，通过 IDB/OPFS 关闭重开、QuickJS 首次执行、错误码往返、run 到文件及 artifact 登记、sys 调用方校验、真实本地网络请求及正文超时、真实 offscreen RPC 版本冲突测试。
- 浏览器证据：`output/playwright/browser-evidence.json`，页面错误与捕获的警告/错误日志均为空。
- 修改的源码完成语法检查及 diff 空白检查。

验证范围有边界：没有调用付费模型，没有验收完整模型驱动的长任务，也没有对四种编辑器完成全面交互与 Office 往返测试。浏览器文件登记测试使用模拟下载内容，真实网络另行测试；隔离浏览器没有启用 userScripts 开关，因此未验证其真实执行成功路径。

## 2. 还有什么可研究的问题

按依赖顺序，建议下一阶段研究以下问题；本报告不代表这些能力已经实现。

| 问题 | 当前缺口 | 应如何取得证据 |
| --- | --- | --- |
| 任务能否跨崩溃恢复 | 调用登记仍在内存，没有持久操作日志、恢复检查点或 exactly-once 保证 | 在派发前后、写文件后、收到回执前主动终止进程，检查能否识别结果未知并避免重复提交 |
| 并发编辑是否真正安全 | 版本前置条件仍可省略；raw guest 写入没有条件校验；没有合并和冲突副本界面 | 统一读改写契约，测试工具与多个编辑器竞争、异步读取期间更新、响应丢失后的重试 |
| 浏览器能处理多大的数据 | 跨进程仍是受限 base64 RPC，持久化仍导出内存快照 | 测量不同文件大小下的内存、延迟与失败阈值，再设计分块、背压、增量提交和孤立文件回收 |
| 页面目标能否持续可信 | tabId 不代表同一文档，截图检查不是原子的；CDP 缺少会话级所有权管理 | 引入 documentId / 导航代次；测试导航、标签切换、debugger 占用及会话结束时的资源释放 |
| 长任务能否稳定交付 | 尚无经过任务验收的上下文压缩、结果外置和重复失败检测 | 建立真实任务集，按交付物正确性、重复操作、恢复成本和停止后未决操作验收 |
| 哪些计算适合留在浏览器 | 尚无本产品的 CPU、内存、WASM / Worker / GPU 基准，也未评估可选本地计算服务 | 先选真实工作负载，再比较成熟开源引擎的性能、许可证、维护状态、打包及集成成本 |

优先做“可恢复任务记录”和“完整版本契约”，再扩大数据吞吐及计算引擎。原因是更强执行能力会同时放大重复操作、丢失数据和资源泄漏的代价。

## 3. 当前为什么要这样改

**浏览器计算环境首先需要可信的数据和执行语义。** 如果保存成功不代表字节落盘，停止不清楚是否停止了副作用，旧编辑器可以覆盖新内容，那么增加更多工具只能让失败更难追踪。本轮先解决这些基础问题。

**文件应该承担数据运输，模型应该决定操作。** `saveTo` 是这一方向的最小可用改动：数据可以进入工作区后由代码处理，再成为交付物。当前仍有 RPC 大小限制，不能把它宣传成无限流式计算。

**复用现有架构比立即换框架更合适。** 本轮继续使用已经集成的 QuickJS、AI SDK、Univer 和 tldraw，使用浏览器原生 IndexedDB 事务、AbortController 与 ReadableStream 解决具体问题，没有新增运行时依赖。引入数据库封装库不能自动解决 OPFS 与元数据的提交顺序；此时更换 agent 框架也不能修复保存竞态。

**“浏览器作为超级计算机”可以作为产品方向，但还不是当前性能结论。** 当前证据支持把浏览器建设成连接登录态页面、网络、文件、代码和编辑器的通用任务环境。超大规模计算、任意本地程序、重启后自动接续及高保真 Office 往返仍需要独立实现与验证。本阶段完成的是可靠性基础和一条可组合的数据链路。
