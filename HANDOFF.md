# 加载 / Git

Chrome 开发者模式 → 加载已解压的扩展程序 → 本文件夹（根上有 `manifest.json`）。显示名：`爪爪 · 完全解放版`。改 `src/` 后在扩展卡片点 **重新加载**。无 `package.json`，日常不跑 npm。

Git：本地 `main`，无 remote。不要 `git push`。不要改 `git config`。

Chrome 当前从桌面路径 `C:\Users\yyy\Desktop\PawWork完全解放版` 加载。

技术地图：[AGENTS.md](AGENTS.md)（脊骨）→ [src/AGENTS.md](src/AGENTS.md)（宿主）→ [src/agent/AGENTS.md](src/agent/AGENTS.md)（工具）。
